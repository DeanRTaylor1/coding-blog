import styles from './command-line-modal.module.css';

const CommandLineModal: React.FC = () => {
    return (
        <input className={styles.commandLineBar} />
    )
}

export default CommandLineModal;